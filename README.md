# Color Feedback to EV3 via MQTT

## Overview
This app receives color feedback via a REST API and sends it to an EV3 robot using MQTT protocol.

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
